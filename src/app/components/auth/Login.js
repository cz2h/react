var React = require('react');

class Login extends React.Component {
    render() {
        console.log(this);
        return (
            <h1>Login Page</h1>
        );
    }
};

export default Login;
